project_id = "ababil-shop"

[api]
port = 54300
schemas = ["public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320

[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = ["http://localhost:3000/auth/callback", "http://localhost:3001/auth/callback"]
jwt_expiry = 3600
enable_signup = true

[storage]
enabled = true
# Default bucket configs will be set up via migrations/seed

[studio]
port = 54323
